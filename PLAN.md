# 杂草检测与激光除草 —— 方案设计

> 记录日期：2026-08-24
> 状态：设计定稿，数据下载与首版训练进行中

---

## 1. 项目目标

在**农田真实场景**中，实时检测杂草，并输出**杂草茎部的切割线位置**（离散点 / 线段），供激光执行机构精确打击杂草茎部，实现精准除草。

## 2. 核心需求指标

| 指标 | 要求 |
|---|---|
| 帧率 | ≥ 30 FPS |
| 精度 | 高（农田真实光照 / 密集 / 遮挡场景） |
| 输出 | 杂草茎部切割线（离散点或线段） |
| 硬约束 | **不得误伤农作物** |

## 3. 模型选型：YOLO11

### 3.1 版本谱系与结论

YOLO 版本号分两条线，务必区分：

- **官方主线（Ultralytics）**：v5 → v8 → **YOLO11** → YOLO26，生态最全、工程化最好。
- **学术/社区衍生**：v6(美团)、v7、v9、v10、v12、v13，精度常更高，但部署兼容 / 文档 / 生态参差（其中 v12/v13 非 Ultralytics 官方，命名在来源间还不一致）。

**选型结论：YOLO11** —— 精度 / 速度 / 生态 / 部署四维最均衡的官方版本，且小目标能力适配杂草茎这类细目标。

### 3.2 三种任务模型分工

| 模型 | 输出 | 在方案中的用途 |
|---|---|---|
| YOLO11 **detect** | 框 + 类别 | 检测「杂草 / 作物在哪」 |
| YOLO11 **pose** | 框 + 关键点 | 茎的**离散点**（沿茎轴线） |
| YOLO11 **seg** | 框 + 实例 mask | 作物**轮廓**（避让用） |

## 4. 整体流水线

```
输入图像
   │
   ├─ ① 定位「打哪里」：模型输出 杂草框 + 茎位置(点/线)
   ├─ ② 知道「哪里不能打」：模型输出 作物轮廓(mask)
   │
   ▼
安全后处理（纯代码，非模型）:
   1. 由茎点生成切割线段（含延长余量，吸收茎细 / 摆动 / 抖动误差）
   2. 作物 mask 膨胀一圈（预留安全余量）
   3. 切割线段 ∩ 膨胀后的 mask → 消除相交段
   ▼
输出：安全、可靠的切割线段
```

关键点：**框只负责「定位杂草在哪」，避让决策用「作物 mask（膨胀后）」，不能用框**。

## 5. 切割方案：离散点 vs 横线

| | 方案1：茎离散切割点 | 方案2：茎位置横线 |
|---|---|---|
| 激光动作 | 精确打几个点 | 沿一条线扫 / 照射 |
| 精度 | 高 | 中 |
| 鲁棒性（茎摆动 / 抖动） | 低 | **高** |
| 误伤 | 几乎无 | 有（需避让处理） |
| 标注成本 | 贵（关键点标注 3~5 倍） | 便宜（框 + 一条线） |

**结论**：激光除草面对的是会动的毫米级小目标（茎随风摆、机器人抖、光斑 1~3mm），**「线」比「点」更靠谱**（打不中比误伤更致命）。工业激光除草机普遍用「小区域扫描 / 短线段」而非精确单点。

**采用方案2 的改良版**：茎位置 → **短线段**（茎宽 ± 几毫米，不横跨整框）+ 作物避让门控。

## 6. 作物避让：mask 而非框

### 6.1 为什么不能用「框 ∩ 框」

框是矩形，作物不是矩形。一个倾斜 / 稀疏的作物，其矩形框会盖住大片空地；杂草长在这片空地、离作物本体还远时，框判断会误报「冲突」→ 激光白关 → 漏除。

### 6.2 三层精度

| 层级 | 相交判断依据 | 效果 |
|---|---|---|
| L0 框 ∩ 框 | 矩形 | ❌ 误报多 |
| L1 mask ∩ mask | 作物轮廓（像素） | ✅ 精确 |
| **L2 mask⊕膨胀 ∩ mask** | 作物轮廓外扩安全区 | ✅✅ 工程正解 |

`膨胀 N 像素` = 激光光斑半径 + 安全距离 + 分割边缘误差 + 茎摆动余量（几个到十几个像素）。

### 6.3 必须注意

1. 作物轮廓必须靠**学出来的 seg 模型**，不能靠颜色——作物和杂草都是绿的，颜色 / 植被指数（ExG）分不出「作物 vs 杂草」。
2. mask 有边缘误差 + 光斑直径 + 摆动，**不能贴边打，必须留膨胀安全区**。

## 7. 数据集：CropAndWeed

- 仓库：[cropandweed/cropandweed-dataset](https://github.com/cropandweed/cropandweed-dataset)（WACV 2023，非商用许可）
- 规模：**8034 张图**，1920×1088 RGB；8 种作物 + 16 类杂草
- 标注格式（每个 `bboxes/*.csv`）：`Left, Top, Right, Bottom, LabelID, StemX, StemY`
  - **StemX / StemY = 茎点**，直接对应方案2 的切割点
  - `labelIds/` 提供**语义分割 mask**（作物 / 杂草轮廓，避让用）
- 目录结构：
  - `data/bboxes/` 8034 个 CSV（框 + 茎点）
  - `data/labelIds/CropAndWeed/` 分割 mask
  - `data/params/` 每图元数据（湿度 / 土壤 / 光照 / 难分度）
  - `data/images/` 图片（10.6GB，4 个 tar 包）

### 7.1 类别映射（转换时采用 CropOrWeed2）

- 作物 → 0，杂草 → 1（二分类）
- **丢弃 label 255（Vegetation「微小/模糊植被」兜底类）**：实测 3.3 万实例、约占总目标 40%，对干净二分类是噪声
- 也丢弃未列入 crop/weed 的类别（如 Courgette、Radish、Asparagus 等非关注作物）

## 8. 硬件与部署

| 项 | 现状 | 影响 |
|---|---|---|
| 开发机 GPU | RTX 3050 Laptop，**4GB 显存** | 只能训 nano/small 档，imgsz 640（OOM 降 512），batch 8~16 |
| 目标部署 | 激光除草机器人（未定） | 最终 30+ FPS 需 Jetson/GPU + TensorRT FP16 |
| 训练计划 | YOLO11n 起步 | 先验证「能否稳定找到杂草」的地基 |

## 9. 关键风险与注意点

1. **手眼标定（最大坑）**：图像像素 → 激光振镜 / 机械臂坐标的变换，两方案都要做，随震动 / 温漂失效需定期重标定。应尽早排入计划。
2. **茎点标注成本**：pose 标注贵 3~5 倍；CropAndWeed 只给单点茎位置（方案2），沿茎完整离散点线（方案1）需自标。
3. **数据集噪声**：40% 目标是模糊小目标（label 255），训练 mAP 会偏低于「干净数据集」，属正常。
4. **许可证**：CropAndWeed 非商用，仅用于研究 / 实验。
5. **字段语义**：茎的「切割点」应按机器人要切的位置定义（基部），而非植物学定义；禾本科杂草茎叶难分。

## 10. 当前进度与下一步

### 已完成（阶段 A · 子集先行验证）
- [x] 数据集选型（CropAndWeed）
- [x] 标注下载 + 格式解析
- [x] 环境安装（uv venv + torch 2.13 cu130 + ultralytics 8.4.127）
- [x] 转换脚本 `convert_cropandweed.py`（作物→0 / 杂草→1，茎点存 `stems.json`，按图划分 train/val）
- [x] 作物避让后处理 `cut_pipeline.py`（mask 膨胀 + 相交消除，单测通过）
- [x] **子集训练 + 评估**：YOLO11n，1739 图 → mAP@0.5=0.758 / mAP@0.5:0.95=0.515，**FPS=99.7**
- [x] 部署 demo 端到端跑通（检测 → 茎线 → 作物避让 → 安全切割线，9 张可视化）

### 进行中
- [ ] 图片全量下载（当前 4000/8034，tar3/4 约 5.4h）

### 下一步（阶段 B）
1. 全量下完 → 重跑转换（~7705 图）→ 完整训练 YOLO11n
2. 补齐 **pose（茎点）** + **seg（作物 mask）** 两个模块，把 demo 里的 GT 替换为模型预测，完成真正端到端
3. 手眼标定（图像 → 激光振镜坐标变换）排期
